# ----------------------------------------------------------------------
# 1. 시스템 및 로컬 환경 파일
# ----------------------------------------------------------------------

# Windows 썸네일 캐시
Thumbs.db
# macOS 메타데이터 파일
.DS_Store

# ----------------------------------------------------------------------
# 2. 파이썬 환경 파일
# ----------------------------------------------------------------------

# 가상 환경 폴더 (Git 추적 제외 필수)
venv/
venv311_flood/

# 컴파일된 바이트코드 (매 실행 시 자동 생성)
__pycache__/
*.pyc
*.pyd

# Pytest 등 테스트 결과 및 로그
.pytest_cache/
htmlcov/
.coverage

# ----------------------------------------------------------------------
# 3. 보안 및 민감 정보 파일 (절대 외부에 노출 금지)
# ----------------------------------------------------------------------

# Firebase Admin SDK 서비스 계정 키 (파일명 대체)
# 이 파일은 환경 변수를 통해 서버에 전달해야 합니다.
floodnavi-f4f2e-firebase-adminsdk-fbsvc-1475fb449d.json

# 기타 API 키/비밀 정보 파일 (존재한다면)
.env
.flaskenv

# ----------------------------------------------------------------------
# 4. 백업 및 임시 파일
# ----------------------------------------------------------------------

# 복사 파일 및 임시 데이터
app copy.py
*.tmp
data.json # 데이터베이스 대신 사용된 임시 데이터 파일일 경우 제외 (선택 사항)
python.py # 임시 테스트 파일로 보임
test.py   # 임시 테스트 파일로 보임

# ----------------------------------------------------------------------
# 5. Git이 인식할 수 없는 파일
# ----------------------------------------------------------------------

# 현재 목록에 포함된 잘못된 확장자의 gitignore 파일 (git이 자동으로 처리)
.gitignore.txt